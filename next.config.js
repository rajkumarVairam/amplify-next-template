/** @type {import('next').NextConfig} */
const nextConfig = {
  // Blog posts and docs are plain .md files read server-side by lib/content.ts
  // using gray-matter. No MDX webpack transform is needed.
};

module.exports = nextConfig;
